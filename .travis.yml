language: python

python:
    - "2.6"
    - "2.7"

before_install:
    - pip --quiet install --use-mirrors numpy
    - pip --quiet install --use-mirrors coverage

install:
    - python setup.py install

script:
    - nosetests
